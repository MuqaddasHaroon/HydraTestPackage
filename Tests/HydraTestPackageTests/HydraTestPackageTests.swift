import XCTest
@testable import HydraTestPackage

final class HydraTestPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(HydraTestPackage().text, "Hello, World!")
    }
}
